(function(window) {
  window.__env = window.__env || {};

  // These values will be replaced at runtime
  window.__env.apiUrl = "${USER_SERVICE_URL}";
//   window.__env.featureFlag = "${FEATURE_FLAG}";
})(this);
