# ─── rustfs namespace: RustFS on its own subdomain (HTTPS only) ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rustfs-ingressroute
  namespace: rustfs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`rustfs.home.lab.local`)
      kind: Rule
      services:
        - name: rustfs-service
          port: 9001
  tls:
    secretName: local-cert-tls

---
# ─── rustfs namespace: Jellyfin on its own subdomain (HTTP + HTTPS) ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin-ingressroute-http
  namespace: rustfs
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`jellyfin.home.lab.local`)
      kind: Rule
      services:
        - name: jellyfin-service
          port: 8096

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin-ingressroute-https
  namespace: rustfs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`jellyfin.home.lab.local`)
      kind: Rule
      services:
        - name: jellyfin-service
          port: 8096
  tls:
    secretName: local-cert-tls

---
# ─── rustfs namespace: qBittorrent on its own subdomain (HTTPS only) ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: qbittorrent-ingressroute
  namespace: rustfs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`qbittorrent.home.lab.local`)
      kind: Rule
      services:
        - name: qbittorrent
          port: 8080
  tls:
    secretName: local-cert-tls

---
# ─── rustfs namespace: Sonarr on its own subdomain (HTTPS only) ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-ingressroute
  namespace: rustfs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.home.lab.local`)
      kind: Rule
      services:
        - name: sonarr
          port: 8989
  tls:
    secretName: local-cert-tls

---
# ─── rustfs namespace: Radarr on its own subdomain (HTTPS only) ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr-ingressroute
  namespace: rustfs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`radarr.home.lab.local`)
      kind: Rule
      services:
        - name: radarr
          port: 7878
  tls:
    secretName: local-cert-tls

---
# ─── rustfs namespace: Prowlarr on its own subdomain (HTTPS only) ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prowlarr-ingressroute
  namespace: rustfs
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`prowlarr.home.lab.local`)
      kind: Rule
      services:
        - name: prowlarr
          port: 9696
  tls:
    secretName: local-cert-tls

---
# ─── technitium-dns namespace: DNS console on its own subdomain ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: technitium-dns-ingressroute
  namespace: technitium-dns
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dns.home.lab.local`)
      kind: Rule
      services:
        - name: technitium-dns-service
          port: 5380
  tls:
    secretName: local-cert-tls

---
# ─── vault namespace: Vault on its own subdomain ───
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vault-ingressroute
  namespace: vault
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`vault.home.lab.local`)
      kind: Rule
      services:
        - name: vault-service
          port: 8200
  tls:
    secretName: local-cert-tls

# ─── kubernetes-dashboard namespace: Dashboard on its own subdomain (HTTPS only) ───
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: dashboard-transport
  namespace: kubernetes-dashboard
spec:
  # Dashboard auto-generates its own self-signed cert on startup.
  # Traefik has no way to validate that cert against a CA, so we
  # tell it to skip verification for this specific backend only -
  # this does not affect the front-facing TLS (local-cert-tls) that
  # the browser sees, only the Traefik -> Dashboard hop internally.
  insecureSkipVerify: true

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard-ingressroute
  namespace: kubernetes-dashboard
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dashboard.home.lab.local`)
      kind: Rule
      services:
        - name: kubernetes-dashboard
          port: 443
          scheme: https
          serversTransport: dashboard-transport
  tls:
    secretName: local-cert-tls