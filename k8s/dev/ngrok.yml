# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: ngrok-tunnel
#   namespace: library-dev
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: ngrok-tunnel
#   template:
#     metadata:
#       labels:
#         app: ngrok-tunnel
#     spec:
#       containers:
#         - name: ngrok
#           image: ngrok/ngrok:latest
#           args:
#             - "http"
#             - "traefik.kube-system.svc.cluster.local:80"
#             - "--url=cotton-overripe-skyrocket.ngrok-free.dev"  # 👈 fixed
#           env:
#             - name: NGROK_AUTHTOKEN
#               valueFrom:
#                 secretKeyRef:
#                   name: ngrok-credentials
#                   key: authtoken
