apiVersion: v1
kind: Namespace
metadata:
  name: technitium-dns

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-dns-pvc
  namespace: technitium-dns
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tailscale-technitium
  namespace: technitium-dns

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: tailscale-technitium
  namespace: technitium-dns
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: tailscale-technitium
  namespace: technitium-dns
subjects:
  - kind: ServiceAccount
    name: tailscale-technitium
    namespace: technitium-dns
roleRef:
  kind: Role
  name: tailscale-technitium
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: technitium-dns
  namespace: technitium-dns
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: technitium-dns
  template:
    metadata:
      labels:
        app: technitium-dns
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      nodeSelector:
        kubernetes.io/hostname: groot-server
      serviceAccountName: tailscale-technitium
      containers:
        - name: technitium-dns
          image: technitium/dns-server:latest
          ports:
            - containerPort: 53
              protocol: UDP
            - containerPort: 53
              protocol: TCP
            - containerPort: 5380
              protocol: TCP
          volumeMounts:
            - name: dns-data
              mountPath: /etc/dns
      volumes:
        - name: dns-data
          persistentVolumeClaim:
            claimName: technitium-dns-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: technitium-dns-admin
  namespace: technitium-dns
spec:
  selector:
    app: technitium-dns
  ports:
    - name: web-admin
      port: 5380
      protocol: TCP
      targetPort: 5380
