apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-deployment
  namespace: library-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-deployment
  template:
    metadata:
      labels:
        app: booking-deployment
    spec:
      containers:
      - name: booking-deployment
        image: hellisback/library-booking-deployment-app:${IMAGE_VERSION}
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: library-config
              key: SPRING_PROFILES_ACTIVE
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: library-config
              key: SPRING_DATASOURCE_URL
        - name: SPRING_DATASOURCE_DRIVER_CLASS_NAME
          valueFrom:
            configMapKeyRef:
              name: library-config
              key: SPRING_DATASOURCE_DRIVER_CLASS_NAME
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: library-secret
              key: SPRING_DATASOURCE_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: library-secret
              key: SPRING_DATASOURCE_PASSWORD
        livenessProbe:
          httpGet:
            path: /booking-deployment/actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /booking-deployment/actuator/health
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 500
---
apiVersion: v1
kind: Service
metadata:
  name: booking-deployment-service
spec:
  selector: 
    app: booking-deployment
    namespace: library-dev
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30003
  type: NodePort
